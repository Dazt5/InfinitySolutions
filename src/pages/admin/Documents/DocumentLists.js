import React from 'react';
import { Layout, DocumentList } from '../../../components/dashboard';

const DocumentLists = (props) => (

    <Layout>
        <DocumentList props={props}/>
    </Layout>
)


export default DocumentLists;