import React from 'react';
import {Layout, CorporationProfiles} from '../../components/dashboard'

const CorporationProfile = (props) => (
    

    <Layout>
        <CorporationProfiles props={props}/>
    </Layout>
)

export default CorporationProfile;
