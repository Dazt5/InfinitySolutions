import React from 'react';
import {Layout, UserProfiles} from '../../components/dashboard'

const UserProfile = (props) => (
    

    <Layout>
        <UserProfiles props={props}/>
    </Layout>
)

export default UserProfile;
